# Environment variables for running the test suite (Vitest).
# Tests don't hit a real Supabase project, so placeholder values are sufficient
# to satisfy the env-var check in src/services/supabaseClient.ts at import time.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder_anon_key
